# Always install peer dependencies automatically
auto-install-peers=true

# Use legacy peer dependency resolution (avoids conflicts)
legacy-peer-deps=true

# Save exact package versions (recommended)
save-exact=true

# Use package-lock.json (ensures consistent installs)
package-lock=true
